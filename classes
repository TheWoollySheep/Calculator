from tkinter import *

class KButtons:
    def _inti_ (self, master):
        frame = Frame(master)
        frame.pack()

        printButton = Button(frame, text = "print message")


root = Tk()
def var1 = (KButtons)
var1.pack()
root.mainloop()
